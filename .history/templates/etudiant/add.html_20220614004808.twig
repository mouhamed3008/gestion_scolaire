{% extends 'base.html.twig' %} {% block body %}
<div class="main-panel">
    <!-- BEGIN : Main Content-->
    <div class="main-content">
        <div class="content-overlay"></div>
        <div class="content-wrapper">
            <div class="row">

                <div class="card-header bg-danger col-12 mt-4">
                    <div class="card-title text-center font-weight-bold">{{ title }}</div>
                    <a href="{{path('app_etudiant')}}" class="btn btn-success btn-sm float-right">
                        Liste des étudiants
                      </a>
                </div>
            </div>

            <div class="card">

                <div class="col-8 offset-2 mt-5">
                    {{ form_start(form, {'attr':{'novalidate':'novalidate'}}) }}
                    <div class="row">
                        <div class="col">{{ form_row(form.etudiant.nomComplet) }}</div>
                        <div class="col">{{ form_row(form.etudiant.email) }}</div>
                    </div>

                    <div class="row">
                        <div class="col">{{ form_row(form.etudiant.sexe, {'attr':{'class':'form-control'}}) }}</div>
                        <div class="col">{{ form_row(form.etudiant.adresse) }}</div>

                    </div>
                    <div class="col">{{ form_row(form.etudiant.password) }}</div>
                    <div class="col">{{ form_row(form.etudiant.matricule) }}</div>
                    <div class="select">

                        {{ form_row(form.classe,{'attr':{'class':'form-control'}}) }}
                    </div>
                    {{form_widget(form.enregistrer, {'attr':{'class':'btn btn-success offset-5 mt-3 mb-5'}}) }} {# <select class="form-control
                    ml-3" name="" id="" style="width:150px">
                    {% for annee in app.session.get('annees') %}
                    <option if(annee.etat==1) 'selected'
                    value="{{annee.id}}">{{annee.libelle}}</option>
                    {% endfor %}
                    </select> {{ form_end(form) }} #}
                </div>
            </div>


        </div>


    </div>
</div>


</div>
</div>



{% endblock %}